#!/bin/sh

preinit_mount_factory_data() {
	local mtd_path

	. /lib/functions.sh
	. /lib/functions/system.sh

	case $(board_name) in
	*)
		echo "Don't support for $(board_name)"
	;;
	esac
}

boot_hook_add preinit_main preinit_mount_factory_data
